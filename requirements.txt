# requirements.txt
# Works on Python 3.11 / 3.12 / 3.13 — all pre-built wheels, no compiler needed.
# pandas-ta removed: all indicators are implemented natively in feature_engineer.py
#
# INSTALL ORDER:
#   Step 1:  pip install -r requirements.txt
#   Step 2:  pip install torch --index-url https://download.pytorch.org/whl/cpu
#
# See install.bat (Windows) or install.sh (Linux) for a one-click installer.

# ── Core data & ML ────────────────────────────────────────────────────────────
numpy>=2.0
pandas>=2.2.3
scikit-learn>=1.5
xgboost>=2.1
joblib>=1.4

# ── Reinforcement learning ────────────────────────────────────────────────────
gymnasium>=0.29
stable-baselines3>=2.3
sb3-contrib>=2.3

# ── Data fetching ─────────────────────────────────────────────────────────────
yfinance>=0.2.40
pyarrow>=16.0

# ── MT5 (Windows only) ───────────────────────────────────────────────────────
# Comment this line out if you are on Linux / Codespaces
MetaTrader5>=5.0.45

# ── Monitoring & utilities ────────────────────────────────────────────────────
matplotlib>=3.9
tensorboard>=2.17
tqdm>=4.66
loguru>=0.7
rich>=13.7
python-dotenv>=1.0
python-dateutil>=2.8
